<!DOCTYPE html>
<html lang="th" class="h-full">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Signup • Cognito</title>

  <!-- ใช้ Tailwind CDN ได้ถ้าต้องการสวยๆ (ลบออกได้ถ้าไม่ใช้) -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-slate-900 text-white flex items-center justify-center p-6">
  <div class="w-full max-w-md">
    <div class="bg-slate-800 rounded-2xl shadow-xl p-6">
      <h1 class="text-2xl font-semibold mb-4">สร้างบัญชีผู้ใช้</h1>
      <p class="text-sm text-slate-300 mb-6">
        กรอกข้อมูลด้านล่างเพื่อสมัครสมาชิก (Cognito SignUp)
      </p>


      <form id="signupForm" class="space-y-4">
        <div>
          <label for="username" class="block text-sm mb-1">Username</label>
          <input id="username" name="username" type="text" required
                 class="w-full rounded-lg bg-slate-700 border border-slate-600 px-3 py-2 outline-none focus:ring-2 focus:ring-indigo-500" />
        </div>

        <div>
          <label for="name" class="block text-sm mb-1">ชื่อ-นามสกุล (Name)</label>
          <input id="name" name="name" type="text" required
                 class="w-full rounded-lg bg-slate-700 border border-slate-600 px-3 py-2 outline-none focus:ring-2 focus:ring-indigo-500" />
        </div>

        <div>
          <label for="email" class="block text-sm mb-1">Email</label>
          <input id="email" name="email" type="email" required
                 class="w-full rounded-lg bg-slate-700 border border-slate-600 px-3 py-2 outline-none focus:ring-2 focus:ring-indigo-500" />
        </div>

        <div>
          <label for="password" class="block text-sm mb-1">Password</label>
          <input id="password" name="password" type="password" required
                 class="w-full rounded-lg bg-slate-700 border border-slate-600 px-3 py-2 outline-none focus:ring-2 focus:ring-indigo-500" />
          <p class="mt-1 text-xs text-slate-400">
            * ต้องตรงตามกฎรหัสผ่านที่กำหนดใน Cognito User Pool
          </p>
        </div>

        <button id="btnSubmit" type="submit"
                class="w-full rounded-lg bg-indigo-600 hover:bg-indigo-500 transition px-4 py-2 font-medium">
          สมัครสมาชิก
        </button>
      </form>

      <!-- สถานะการส่ง -->
      <div id="status" class="mt-4 hidden">
        <div id="statusText" class="text-sm"></div>
        <pre id="statusData" class="mt-2 text-xs bg-slate-900 p-3 rounded-lg overflow-auto"></pre>
      </div>
    </div>

    <p class="text-center text-xs text-slate-400 mt-4">
      มีโค้ดยืนยันแล้ว? ไปที่หน้า <a href="/confirm" class="underline">ยืนยันอีเมล</a>
    </p>
  </div>

  <script>
    const form = document.getElementById('signupForm');
    const btn = document.getElementById('btnSubmit');
    const statusBox = document.getElementById('status');
    const statusText = document.getElementById('statusText');
    const statusData = document.getElementById('statusData');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      // อ่านค่าจาก form
      const username = document.getElementById('username').value.trim();
      const email    = document.getElementById('email').value.trim();
      const password = document.getElementById('password').value;

      // ปิดปุ่มกันกดย้ำ
      btn.disabled = true;
      btn.textContent = 'กำลังสมัคร...';

      statusBox.classList.remove('hidden');
      statusText.className = 'text-sm text-slate-300';
      statusText.textContent = 'กำลังส่งข้อมูล...';
      statusData.textContent = '';

      try {
        const name = document.getElementById('name').value.trim();
        const res = await fetch('/signup', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ username, password, email, name })
        });

        const json = await res.json();

        if (res.ok) {
          statusText.className = 'text-sm text-emerald-400';
          statusText.textContent = 'สมัครสมาชิกสำเร็จ! กรุณาเช็คอีเมลสำหรับโค้ดยืนยัน';
          statusData.textContent = JSON.stringify(json, null, 2);
          form.reset();
        } else {
          statusText.className = 'text-sm text-red-400';
          statusText.textContent = json.error || 'เกิดข้อผิดพลาดในการสมัครสมาชิก';
          statusData.textContent = JSON.stringify(json, null, 2);
        }
      } catch (err) {
        statusText.className = 'text-sm text-red-400';
        statusText.textContent = 'ไม่สามารถเชื่อมต่อเซิร์ฟเวอร์ได้';
        statusData.textContent = String(err);
      } finally {
        btn.disabled = false;
        btn.textContent = 'สมัครสมาชิก';
      }
    });
  </script>
</body>
</html>
